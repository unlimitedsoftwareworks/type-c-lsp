// Valid interface join - methods with different signatures (overloading allowed)

type Serializable<T> = interface {
    fn serialize() -> T
    fn serialize(format: T) -> T  // Overload is fine
}

type Drawable = interface Serializable<string>{
    fn draw() -> void
}


fn main(){
    let e: Drawable? = null
    let z = e.serialize()
}
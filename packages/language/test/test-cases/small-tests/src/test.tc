cfn generator<T>(data: T[]) {
    for let i = 0u64; i < data.length; i++ {
        yield data[i];
    }
}

cfn gen() {
    yield {x: 1u32, y: "hi"}
    yield {x: 2u32, y: "hello", z: 10.f}
}

fn generator_test() {
    let numbers = [10, 20, 30, 40, 50]
    let gen = generator(numbers)
}

type Test = class {
    let x: u32 = 1
    fn init() {
    }
}
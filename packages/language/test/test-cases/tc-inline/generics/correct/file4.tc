// @tc-expect-no-errors

type Container<T> = interface {
    fn slice(end: u64) -> Container<T>
}

type Array<T> = class {
    fn slice(end: u64) -> Array<T> {
        return new Array<T>()
    }
}

fn main() -> void {
    let arr2: Container<u32> = new Array<u32>()
}
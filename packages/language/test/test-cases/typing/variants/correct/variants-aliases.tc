type Option<T> = variant {
    Some(value: T),
    None
}

type ServerOk = Option.Some<u32>
type ServerErr = Option.None<string>

fn main() -> void {
    let ok: ServerOk<u32> = ServerOk(200u32)
    let ok2 = Option.Some(200u32)

    ok = ok2

    let err: ServerErr<string> = ServerErr()
}
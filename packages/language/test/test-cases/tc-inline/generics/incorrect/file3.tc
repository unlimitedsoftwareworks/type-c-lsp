type Container<T> = interface {
    fn slice(end: u64) -> Container<T>
}

type Array<T> = class {
    fn slice(end: u64) -> Array<T> {}
}

fn main() -> void {
    /// @Error(TCE007): string is not assignable to u32
    let arr2: Container<u32> = new Array<string>()
}
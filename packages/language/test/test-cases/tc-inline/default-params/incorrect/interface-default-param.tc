// Test: Interface methods cannot have default parameters

type Bad = interface {
    /// @Error(TCE029): Interface methods cannot have default parameter values
    fn process(a: u32, b: u32 = 0u32) -> void
}

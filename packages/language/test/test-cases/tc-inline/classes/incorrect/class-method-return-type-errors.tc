// Test class method return type validation - error cases

type Calculator = class  {
    // OK: Explicit type matches
    fn good() -> u32 = 42

    // OK: Can infer type
    fn inferredOk() = 42u32

    /// @Error(TCE044): Cannot infer common type
    fn mixedReturns(flag: bool) =
        if flag => 1u32
        else "error" // @error TCE044
    

    /// @Error(TCE045): Method return type mismatch
    fn wrongBlockType() -> string {
        return 42
    }
}

fn x() -> u32 = 42
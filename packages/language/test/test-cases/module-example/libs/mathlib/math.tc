namespace math
{
    fn abs(x: i32) -> i32 {
        if x < 0 {
            return -x
        }
    	
        return x
    }

    type Animal<U, V> = variant {
        Kiisu(name: U),
        Neutsu(name: V),
    }
}
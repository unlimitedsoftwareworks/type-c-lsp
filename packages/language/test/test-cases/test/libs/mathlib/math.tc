namespace math
{
    fn abs(x: i32) -> u32 {
        if x < 0 {
            return -x as u32
        }
    	
        return x as u32
    }

    type Animal<U, V> = variant {
        Kiisu(name: U),
        Neutsu(name: V),
    }
}
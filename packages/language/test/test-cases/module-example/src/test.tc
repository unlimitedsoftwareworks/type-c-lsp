from fib import fib
from mathlib.math import math

namespace Zelda {
    type Triangle = u32
}

type Square = u32
type Cat = math.Animal.Kiisu

type C = class {
    /**
     * Returns some value idk
     */
    static fn get() -> u32 {
        return 123
    }
}

fn main() {
    let z = fib(10)
    let a = math.abs(10)
    let b: enum as u32 {Red = 1, Green = 2} = 2 as enum as u32 {Red = 1, Green = 2}
    let c = math.Animal.Kiisu(123)
    let y = C.get()

    let vec = {x: 1u32, y: 1u32, z: 1u32}

    let z = vec.{
        x: 1,
        y: 1,
        z: 1
    }
}

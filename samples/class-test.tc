type Array<T> = class {
    let data: T[]
    let capacity: u64
    let length: u64

    fn init(capacity: u64) {
        this.data = []
        this.capacity = capacity
        this.length = 0
    }

    fn print() -> void {
        return this.data
    }

    fn clone() -> Array<T> {
        return new Array<T>(this.capacity)
    }

}

fn test() {
    let arr = new Array<Array<u32> >(10)
    let c = arr.clone()
    let z: u32[] = [1, 2]
    let y = z.length
}
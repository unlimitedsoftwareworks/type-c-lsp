// Test file to verify expression error type validation

fn testErrors() {
    // Test 1: Unresolved reference
    let x = unknownVariable;
    
    // Test 2: Array without context (cannot infer type)
    let arr = [];
    
    // Test 3: Invalid member access
    let num = 42;
    let invalid = num.toString;
    
    // Test 4: Invalid operation
    let str = "hello";
    let result = str[0].unknown_method();
}
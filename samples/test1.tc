type Point = {
    x: u32, y: u32, z: u32
}

fn sum(p: Point) -> u32 {
    return p.x + p.y + p.z + 1
}

fn x<T>(arr: T[]) -> T {
    let x = arr.resize(300) 
    return x
}  

let z = x<u32>([1, 2, 3])
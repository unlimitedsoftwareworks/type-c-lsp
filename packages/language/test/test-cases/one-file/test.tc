// Comprehensive test cases for struct contextual typing

type Point2D = struct {
    x: f32,
    y: f32
}

type Point3D = struct {
    x: f32,
    y: f32,
    z: f32
}

// Test 2: Struct spread with contextual typing
fn test2() {
    let base: Point2D = {x: 1.0f, y: 2.0f}
    let extended = {...base, z: 3.0f}
}
type Data = interface {
    fn getValue() -> u32
}

type Container = interface {
    fn getData() -> Data
}

fn testChaining() {
    let c: Container? = null
    
    let value = c?.getData().getValue() 
}
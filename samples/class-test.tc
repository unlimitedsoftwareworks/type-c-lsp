type Array<U> = class {
    let data: U[]
    let capacity: u64
    let length: u64

    fn init(capacity: u64) {
        this.data = []
        this.capacity = capacity
        this.length = 0
    }

    fn print() -> void {
        return this.data
    }

    fn clone() -> Array<U> {
        return new Array<U>(this.capacity)
    }

}

fn test() {
    let arr = new Array<Array<u32> >(10)
    let z = arr?.data
    let c = z
}